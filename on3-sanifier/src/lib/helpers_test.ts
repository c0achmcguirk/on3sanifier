import { On3Helpers } from './helpers';

describe('On3Helpers', () => {
  it('should be able to be instantiated', () => {
    const helpers = new On3Helpers();
    expect(helpers).toBeDefined();
  });
});
